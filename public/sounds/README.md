# Sound Notifications

Place your notification sound file here as `notification.mp3` or `notification.wav`.

If no sound file is provided, the system will use a browser-generated beep sound as fallback.
